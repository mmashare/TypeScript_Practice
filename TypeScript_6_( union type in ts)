 //// union type in ts (it's means that you can gives tht peremeter a string type or number (:this | that))
function printStatusCode(code: string | number) {
  console.log(`My status code is ${code}.`)
}
printStatusCode(404);
printStatusCode('404');
